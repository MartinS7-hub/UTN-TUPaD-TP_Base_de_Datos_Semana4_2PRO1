| Campo | Contenido |
| --- | --- |
| Herramienta | OpenCode / Big Pickle |
| Spec o prompt utilizado | “Necesito un script SQL para PostgreSQL que inserte 50.000 filas en articulo, distribuidas de forma pareja entre los rubros existentes, con precios entre 500 y 5000 y stock aleatorio entre 0 y 200, 20.000 clientes y 200.000 compras con sus detalles. Usá generate_series y no uses PL/pgSQL si no es necesario. No modifiques ninguna otra tabla.” <br> Se indicó además: respetar schema.sql (CHECK, UNIQUE, FK, ENUM), no tocar rubro, y generar TP3/Parte 1/carga_masiva.sql con lógica set-based. |
| ¿Qué generó? | Archivo TP3/Parte 1/carga_masiva.sql completo: 1 INSERT de 50.000 articulos (generate_series, valor 500-5000, unidades 0-200, reparto parejo entre rubros con ((gs-1)%4+1)), 1 INSERT de 20.000 clientes, 1 INSERT de 200.000 compras, 1 INSERT de 200.000 detalle_compra (1 detalle por compra), UPDATE para recalcular montos, bloque BEGIN/COMMIT y SELECT de verificación. |
| ¿Qué se aceptó? | La lógica set-based con generate_series sin loops PL/pgSQL; la distribución pareja por rubro; los rangos de la consigna (valor 500-5000, unidades 0-200); el patrón con array_agg(id) para elegir cliente_id y articulo_id reales y evitar el error FK 23503 por huecos de IDENTITY; la decisión de 1 detalle por compra para garantizar que toda compra tenga detalle. |
| Qué se modificó, descartó y por qué | Se descartó la iteración 1 completa (224 líneas): no cumplía los rangos pedidos (valor 50-2050, unidades 0-500) y usaba 3 bloques de detalle con ON CONFLICT DO NOTHING, que ocultaba duplicados y dejaba ~159.842 detalles en vez de 200.000. Se reescribió como versión simple set-based. Además se corrigió el UPDATE de montos de data.sql (calificando el id de la subconsulta como c.id) para que los totales quedaran consistentes con SUM(parcial). |
| Verificación realizada | Ejecución completa en PostgreSQL 18 sobre una copia de trabajo: 50.010 articulos, 20.005 clientes, 200.005 compras y 200.010 detalles; 0 compras con monto distinto de SUM(parcial). Se probó dentro de transacción (BEGIN;...ROLLBACK;) según protocolo y se ejecutó ANALYZE sobre las 4 tablas antes de medir. Los conteos dieron el resultado esperado. |